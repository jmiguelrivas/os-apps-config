# Config

## NVIM

Install Dependencies

```sh
npm install -g prettier eslint_d @vue/language-server typescript typescript-language-server
```

Install Packages
```sh
sudo apt install lazygit
```

Link Creation
```sh
ln -s /home/pandora/git_proj/config/nvim /home/pandora/.config/nvim
```